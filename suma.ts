/**
 * Esta es una clase de ejemplo.
 * Falta documentación detallada y etiquetas necesarias.
 */
class ExampleClass {
    /**
     * Metodo sin documentación adecuada.
     */
    exampleMethod(param1: string, param2: number): string {
        return `Param1: ${param1}, Param2: ${param2}`;
    }

    /**
     * Propiedad sin documentación.
     */
    exampleProperty: string = 'Hello World';
}
